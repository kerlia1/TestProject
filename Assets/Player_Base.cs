using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Player_Base : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("git работает?");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
